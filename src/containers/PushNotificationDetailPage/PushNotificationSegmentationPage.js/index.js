import { connect } from 'react-redux';
import PushNotificationSegmentationPage from './PushNotificationSegmentationPage';

const mapsStateToProps = state => ({
});

const mapsDispatchToProps = dispatch => ({
});

export default connect(
	mapsStateToProps,
	mapsDispatchToProps
)(PushNotificationSegmentationPage);

