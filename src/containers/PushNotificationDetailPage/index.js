import { connect } from 'react-redux';
import PushNotificationDetailPage from './PushNotificationDetailPage';

const mapsStateToProps = state => ({
});

const mapsDispatchToProps = dispatch => ({
});

export default connect(
	mapsStateToProps,
	mapsDispatchToProps
)(PushNotificationDetailPage);

