import { connect } from 'react-redux';
import CouponSegmentationPage from './CouponSegmentationPage';

const mapsStateToProps = state => ({});

const mapsDispatchToProps = dispatch => ({});

export default connect(
	mapsStateToProps,
	mapsDispatchToProps
)(CouponSegmentationPage);

