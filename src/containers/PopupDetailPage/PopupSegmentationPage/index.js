import { connect } from 'react-redux';
import PopupSegmentationPage from './PopupSegmentationPage';

const mapsStateToProps = state => ({
});

const mapsDispatchToProps = dispatch => ({
});

export default connect(
	mapsStateToProps,
	mapsDispatchToProps
)(PopupSegmentationPage);

