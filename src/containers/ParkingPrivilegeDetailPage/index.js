import { connect } from 'react-redux';
import ParkingPrivilegeDetailPage from './ParkingPrivilegeDetailPage';

const mapsStateToProps = state => ({});

const mapsDispatchToProps = dispatch => ({});

export default connect(
	mapsStateToProps,
	mapsDispatchToProps
)(ParkingPrivilegeDetailPage);

