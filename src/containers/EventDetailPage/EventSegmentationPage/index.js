import { connect } from 'react-redux';
import EventSegmentationPage from './EventSegmentationPage';

const mapsStateToProps = state => ({});

const mapsDispatchToProps = dispatch => ({});

export default connect(
	mapsStateToProps,
	mapsDispatchToProps
)(EventSegmentationPage);

